spring.application.name=parttimes-backend
server.port=8080

# Cosmos DB MongoDB API Configuration (for prod)
spring.data.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.database=${MONGODB_DATABASE:parttimes-backend}
