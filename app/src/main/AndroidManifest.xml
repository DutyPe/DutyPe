<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Essential Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Location Permissions - Runtime request with clear explanation -->
    <!-- Used for: Showing nearby jobs, calculating distance to workplace -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    
    <!-- Notification Permission - Runtime request (Android 13+) -->
    <!-- Used for: Job alerts, application status updates, employer messages -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Camera Permission - Runtime request only when needed -->
    <!-- Used for: Profile photo capture, QR code scanning for work verification -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
    
    <!-- WAKE_LOCK - Auto-granted by WorkManager, not shown to users -->
    <!-- Used for: Background job sync and notification delivery -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

	<application
		android:name="com.example.dutype.DutyPeApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DutyPe"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">
        
        <!-- Google Play Services version metadata (required for Google Sign-In) -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        
        <!-- Disable Phenotype API (A/B testing) - we don't use it, prevents DEVELOPER_ERROR -->
        <meta-data
            android:name="com.google.android.gms.phenotype.service.DISABLE"
            android:value="true" />
        
        <!-- Disable unnecessary Google Play Services features to reduce startup time -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="" />
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="" />
        
        <!-- Disable WebView camera/media access on startup (saves 500ms) -->
        <!-- Camera is only needed in profile setup screens, not on app launch -->
        <meta-data
            android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="false" />
        <meta-data
            android:name="android.webkit.WebView.MetricsOptOut"
            android:value="true" />
        
        <!-- Google Mobile Ads App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5503082977524600~8481368080" />
        
        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
		<activity
			android:name="com.example.dutype.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DutyPe.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep Link: dutype://job/{jobId}, dutype://worker/{id}, dutype://refer/{code}, etc. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="dutype" android:host="job" />
                <data android:scheme="dutype" android:host="worker" />
                <data android:scheme="dutype" android:host="refer" />
                <data android:scheme="dutype" android:host="application" />
                <data android:scheme="dutype" android:host="employer" />
                <data android:scheme="dutype" android:host="chat" />
                <data android:scheme="dutype" android:host="profile" />
                <data android:scheme="dutype" android:host="notifications" />
                <data android:scheme="dutype" android:host="home" />
            </intent-filter>
            
            <!-- Web Links: https://dutype.in/* (Android App Links - VERIFIED) -->
            <!-- CRITICAL: android:autoVerify="true" enables automatic app opening without chooser dialog -->
            <!-- IMPORTANT: Only HTTPS is used for App Links (HTTP removed to fix Android 11 verification) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Primary domain -->
                <data android:scheme="https" android:host="dutype.in" />
                <!-- Legacy Firebase domain for backward compatibility -->
                <data android:scheme="https" android:host="dutypeapp.web.app" />
            </intent-filter>
        </activity>
        
        <!-- Firebase Cloud Messaging Service - Enterprise Grade -->
        <service
            android:name="com.example.dutype.services.DutyPeMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        
        <!-- FileProvider for camera capture -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <!-- Disable default WorkManager initializer to use Hilt's HiltWorkerFactory -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
