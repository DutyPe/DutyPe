package com.parttimes.backend.worker.models;

public enum ApplicationStatus {
    PENDING,
    REVIEWED,
    SHORTLISTED,
    INTERVIEW_SCHEDULED,
    INTERVIEWED,
    SELECTED,
    REJECTED,
    WITHDRAWN
}
