{
  "servers": {
    "firebase": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "firebase-tools@latest", "mcp"]
    }
  },
  "description": "Firebase Model Context Protocol (MCP) integration for DutyPeApp",
  "purpose": "Enables direct Firebase operations including Firestore, Auth, Crashlytics, and App Check management",
  "projectDetails": {
    "projectId": "dutypeapp",
    "projectName": "DutyPeApp",
    "publicFacingName": "project-1062348180452"
  },
  "requirements": {
    "nodeVersion": ">=16.0.0",
    "firebaseTools": "latest",
    "firebaseProject": "dutypeapp",
    "cli": "Firebase CLI must be installed and authenticated"
  },
  "setup": {
    "install": "npm install -g firebase-tools",
    "login": "firebase login",
    "selectProject": "firebase use dutypeapp"
  }
}
